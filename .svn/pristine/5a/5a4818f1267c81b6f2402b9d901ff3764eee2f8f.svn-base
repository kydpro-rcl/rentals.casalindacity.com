// JavaScript Document

<!--
// Nannette Thacker http://www.shiningstar.net
function confirmSubmit()
{
var agree=confirm("Are you sure you wish to continue?");
if (agree)
	return true ; //aceptar
else
	return false ; //cancelar
}

function muestraConfirm(total, deposit)
	{
		
		//deposit=document.test.deposit.value;
		
		//alert(deposit);
		//alert(total);
		
		if (parseInt(deposit) > parseInt(total))
		{
			//	alert("deposito mayor");
			var pregunta = "Deposit is bigger than reservation amount. Do you wish continue?";
			var respuesta = confirm(pregunta);
			
			if(respuesta) // Quiere decir que dio click en Aceptar
			{
				//window.close();
				return true; //SI ACEPTAR continua las instrucciones
			}else{
	
			  return false;               // SI CANCELAR permanece en la misma ventana
			 }
		}else{
			return true; //CONTINUA SIN HACER PREGUNTAS
		}
	} //END FUNCTION
	
function confirmSubmitText(mensaje)
{
var agree=confirm(mensaje);
if (agree)
	return true ; //aceptar
else
	return false ; //cancelar
}	
// -->

